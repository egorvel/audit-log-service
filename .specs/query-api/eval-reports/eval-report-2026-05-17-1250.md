# Specs self-evaluation — query-api

Date: 2026-05-17-1250
Evaluated: `.specs/query-api/{requirements,design,tasks}.md` (state at HEAD of `hooks/spec-self-eval-stop`).

| # | Check | Verdict |
| - | ----- | ------- |
| 1 | Each AC is testable. | PASS |
| 2 | Tasks have refs and DoD. | PASS |
| 3 | Dependencies between tasks are explicit. | PASS |
| 4 | Every AC in requirements.md is addressed by at least one section in design.md. | PASS |
| 5 | Every AC traces to at least one task whose DoD would fail if the AC regressed. | PASS |
| 6 | No contradictions between requirements.md, design.md, and tasks.md (same fact, same answer everywhere). | PASS |
| 7 | Cross-references resolve: every §X.Y, AC…, or "see Section …" points to a real section that says what's claimed. | PASS |
| 8 | Every AC is in EARS form (Ubiquitous / Event-driven / Unwanted / State-driven / Optional). | PASS |

---

## 1. Each AC is testable. — **PASS**

All 26 ACs map to observable request, response, ordering, pagination, or storage behavior.

## 2. Tasks have refs and DoD. — **PASS**

All 7 tasks have `**Refs.**` and `**DoD.**` blocks.

## 3. Dependencies between tasks are explicit. — **PASS**

`tasks.md` includes a dependency graph plus prose describing task ordering.

## 4. Every AC in requirements.md is addressed by at least one section in design.md. — **PASS**

Each AC has corresponding API, validation, data model, pagination, or integration design coverage.

## 5. Every AC traces to at least one task whose DoD would fail if the AC regressed. — **PASS**

Every AC reaches at least one task with DoD coverage for the relevant behavior.

## 6. No contradictions between requirements.md, design.md, and tasks.md (same fact, same answer everywhere). — **PASS**

No same-fact contradictions were found across the three spec files.

## 7. Cross-references resolve: every §X.Y, AC…, or "see Section …" points to a real section that says what's claimed. — **PASS**

All checked section, AC, AGENTS.md, and local file references resolve.

## 8. Every AC is in EARS form (Ubiquitous / Event-driven / Unwanted / State-driven / Optional). — **PASS**

Every AC has a recognized EARS label and matching sentence shape.

## Summary

All checklist items passed.
